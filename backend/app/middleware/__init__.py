"""
__init__.py

This is an empty __init__.py file for the `middleware` package. 

Purpose:
    This file is used to mark the directory as a Python package, allowing the inclusion 
    of middleware functions and modules into the project. 

Usage:
    You can import modules and functions from this package in other parts of the application.

Example:
    To use a middleware function from this package, you can import it as follows:
    from app.middleware.token_validation import verify_token
"""
